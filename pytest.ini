[pytest]
addopts = -q --ignore=tests/integration --ignore=tests/scenario --ignore=scripts
markers =
    live: tests that hit real external AI provider endpoints
    integration: integration tests requiring valid API keys
    scenario: end-to-end scenario tests for project types
